"""流程层：把「页面判定」翻译成「机械臂动作」。

分层纪律：
    * vision 只判定、不动作；devices 只动作、不决策；domain 只算数、不碰设备；
    * 只有本层可以「先判定再动作」，且每个动作后必须重新取帧判定（点了≠成功）。

坐标纪律（全工程只有这一套）：
    一切点击/滑动坐标都是 **fast 帧（480x640）坐标**。
    OCR 跑在 raw 帧上时，结果必须乘 (fast/raw) 换算回来再交给机械臂。
"""
