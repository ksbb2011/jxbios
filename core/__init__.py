"""核心包。

分层纪律：
    domain  —— 纯业务模型，不 import 任何设备/视觉模块，可独立单测
    vision  —— 只产出「判断结果」，不做业务决策
    flow    —— 只做「看到 A 就做 B」的编排
    devices —— 唯一的副作用出口（机械臂/摄像头）
    infra   —— 配置、轨迹、数据库、通知
"""

# 版本号：每次改动代码后**必须递增**，并在下方记录改动摘要。
# 为什么必须有：Python 模块在进程启动时导入，GUI 运行期间改 .py 文件不会热重载
# （只有 config/*.json 会）——曾出现「代码已改但跑的还是旧逻辑」，日志里却看不出来。
# 版本号会打在启动日志第一行，一眼就能确认当前跑的是哪一版。

# ---- 变更记录（新在最上）----
# 1.1.18 (2026-09-08)
#   * 重复点降噪：扫描钩子把「疑似同一张卡」跳过提示从 warning 降级为 debug，
#     清理日志刷屏（实际去重保险仍是 dup 判定 + ctx.bump 计数器，行为不变）。
#     监护 repeat_point 规则为 counter_delta(action=warn)，数的是 bump 计数器，
#     降级日志不影响监护触发，仅减少日志噪音。
# 1.1.17 (2026-09-08)
#   * GUI 右上角新增「运行监护（异常自动急停）」勾选开关：写 runtime.watchdog.enabled
#     并对运行中的任务即时生效（直接改 watchdog 实例 enabled），调参/排查时可临时关掉。
# 1.1.16 (2026-09-08)
#   * 流程顺序修正：关听单前置到「设置路线」之前（原为先设路线、后关听单，
#     导致设路线全程听单开着、列表被自动刷新干扰，入口/网格容易点偏）。
#     修在 machine._ensure_list_ready（设路线的最早时机），幂等可重复调用。
#   * unknown 恢复新增：识别城市面板「自定义装货点/卸货点」标签页，自动点
#     「出发地/目的地」页签切回省市网格（右滑/返回键对该页无效，此前会卡死
#     并回桌面重来）。
# 1.1.15 (2026-09-08)
#   * 版本号按用户要求定为 1.1.15
#   * 打包封装：PyInstaller → RobotPicker.exe（GUI）+ RobotPickerTools.exe（自检/标定/采模板）
#     + 安装.bat / 卸载.bat / 部署说明.txt；目标机无需安装 Python
#   * 修复打包后主界面闪退：_reboot_into_capable_python 在 frozen 环境直接返回
#   * config_store 增加打包感知：优先 exe 同级 config（可写，标定结果要落盘），
#     并支持 ROBOT_PICKER_HOME 环境变量
#   * ActionKit 补 sleep() 兜底（历史坑：detail.py 调 kit.sleep 抛 AttributeError →
#     详情第二屏读不到 → 价格取错 → 漏单）
#   * detail.py：kit.sleep → 可中断的 ctx.sleep；详情无价格时回退用列表卡片价格
#   * 返回上一级改重试 2 轮；入口定位过滤 OCR 噪声与箭头「→」
# 0.2.4 (2026-09-08)
#   * 修复 dest 入口点到箭头「→」：OCR 常把箭头认成 '1'/'-'；主路径因「杭州..」带
#     噪点判不过 is_entry_text 导致候选不足 → 落到整行聚类兜底，而兜底未过滤，
#     取到的第二个就是箭头。现：① is_entry_text 先清洗非中英文数字再判；
#     ② 兜底聚类同样过滤，不足两个返回 None 走模板（点错比不点更糟）。
#   * ActionKit 补 sleep()：detail.py 的 kit.sleep 修复被还原两次，加兜底实现后
#     即使再被还原也不会抛 AttributeError（详见 actions.py 注释）。
#   * 重新应用 detail.py 的 ctx.sleep 与价格卡片回退（第三次被还原）。
# 0.2.3 (2026-09-07)
#   * GUI 日志落盘：data/logs/run_*.log（原先只有命令行 main.py 写）。城市选择细节走
#     kit._emit 只进 GUI 回调、不进 trace，导致「路线设置失败」查不到过程。
#   * 返回上一级改为重试 2 轮（间隔 0.8s 重取帧）：单帧 OCR 抖动会让整条 dest 路线
#     报废（dest 连选 3 市需回退 2 次）。仍失败才停，不恢复盲点固定坐标。
#   * 返回上一级诊断日志提到 info 级，便于 trace/日志文件留痕。
# 0.2.2 (2026-09-07)
#   * 重新应用 0.2.0 中丢失的 detail.py 两处修复（详情第二屏 ctx.sleep / 价格卡片
#     回退）——detail.py 曾被单独还原回旧版（其余文件均在，疑似编辑器旧副本覆盖），
#     23:11 那轮日志再现 28 次 sleep 报错可证。
#   * 到底判据补关键词「加载完毕/已经全部加载」：真机另一到底文案（用户截图确认），
#     旧 4 词均不命中。
# 0.2.1 (2026-09-07)
#   * 列表到底策略改为 next_route（config runtime.list_no_more.action），到底切下一条路线
#   * 修复「切路线但没重设城市」：next_route 分支原只 refresh_list，App 出发地/目的地
#     筛选仍是上一条路线、拉到的还是旧货源。新增 ctx.route_setup_pending，由主循环
#     下一轮执行 setup_routes() 真正重设始发地/目的地。
# 0.2.0 (2026-09-07)
#   + 详情读不到价格时，回退用列表卡片上的价格（OrderCard.price / ctx.last_card）
#   + 单价统一保留两位小数（unit_price 源头 round）
#   * 修复详情页第二屏读取失败：kit.sleep → ctx.sleep（ActionKit 无 sleep 方法，
#     17/17 次静默降级，导致价格被 OCR 取成车长等错值 → 单价近 0 误判淘汰 = 漏单）
#   * 修复上滑卡顿/搓屏：贝塞尔控制点改为按行进方向插值（原写法控制点会落到
#     反方向，轨迹先折回再走）
#   * 城市选择改为自适应定位：grid_band() 按「历史地区/请选择X」文字现场测网格
#     上下界，不再用硬编码屏幕比例（曾点成历史地区里的江苏/浙江）
#   * 返回上一级：全屏 OCR 语义定位，失败即停（不再盲点固定坐标 + 继续乱滑）
#   * 上滑幅度与时长同步 ×1.4（保持速度不变，避免惯性变大翻过头）
__version__ = "1.1.18"
