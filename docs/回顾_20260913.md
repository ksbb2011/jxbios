# 开发回顾 · 2026-09-13（周日）｜robot_order_picker_imouse

> 版本 v1.2.0｜今日主线从「算法调优」转向「交付化 + 校准体系闭环」：出 exe、做 iOS 传感 App、整屏网格标定、点击残差补偿、arm_range 自动同步，并 git 初始化（4 提交）。

## 1) 当日完成项与验证结果

| 改动点 | 位置 | 验证手段 | 结果 |
|---|---|---|---|
| **iOS 标定 App（云端编译交付）** | `ios_calib/`(SwiftUI) + `project.yml`(XcodeGen) + `Info.plist` + `build_ipa.sh` + `.github/workflows/ios-ipa.yml` + README | 三条路线：本地/免费 Apple ID 自签 / GitHub Actions 免费编译 / 加固 App 协议核对 | ✅ |
| **整屏网格标定（手机 App 当传感器）** | `tools/calibrate_full_grid.py`(新) + `calib_mock_phone.py`(离线自测 13 断言) + `calib_common.py` | 离线全过；真机第 3 轮 mean 0.09/max 1.33pt | ✅ 精度最高一条路 |
| **点击落点残差补偿 tap_correction** | `core/devices/arm.py::click_pixel()`（半径内最近锚点平移）| 运行日志现 `补偿[city_clear -6,+0]`；修掉"底部按钮偏左 6~7pt" | ✅ |
| **arm_range 自动同步 + 补救入口** | `calibrate_arm_ink.py` + `--sync-arm-range`(不取帧秒级补) + `calib_common` | 修 09-10「返回箭头点不到」根因 | ✅ |
| **映射标定增强（墨点版）** | `calibrate_arm_ink.py`：canvas 范围校验 ±6pt + arm_range 自动重算 | 防"笔戳画布外→界面变化被当墨点→z_press 算偏浅" | ✅ |
| **映射标定收尾 NameError 修复** | `_report()` width/height 改由调用方传入 | 达标标定不再异常收尾 | ✅ |
| **城市面板网格扫描参数 + 诊断** | `actions.py::ensure_text_visible()` + `city_picker.py` + `city_grid_scan.py` | 10:xx 多段跑单验证「网格定位-全屏文字」逐屏选省/市 | ✅ |
| **PyInstaller 打包（UAC + 双 exe）** | `installer/build_exe.spec` + `build_exe.bat` + `校准工具.exe` | dist 出 2 exe(312MB)；gui_boot.log ③ onnxruntime: IMPORT_OK | ✅ |
| **VC++ 运行库冲突根治（血泪约定 #4）** | spec 剔除 PyQt5\Qt5\bin 旧 MSVCP140 + System32 补齐 + `gui/app.py` ctypes 预载 | 防 [WinError 1114]→OCR 全废；全包运行库仅 1 套 | ✅ |
| **templates.json 路径修复** | 24/44 path 补 `templates/` 层（备份 `templates.json.bak_20260913`）| 42/44 可解析；澄清 24 条 `enabled:false`→**不改运行行为** | ✅（仅资产登记） |
| **校准文档 + 打包说明 + Xcode 指南** | `docs/新电脑_校准指南.md` / `installer/打包与交付说明.md` / `docs/Xcode_英文界面操作指南.md` | 文档落地 | ✅ |
| **git 初始化** | `.gitignore` + `.gitattributes` + 4 提交 | `git log` 见 4 条 | ✅ |

**跑单验证（今日机器人连跑）**：
- run_20260913_085514（H 路线，1217s）：cards=164/命中25/详情17/hold=11/resumed=4/orders=33/pindan=3/reject=9/**route_setup_fail=2**/list_no_more=1。
- run_20260913_094822（J 路线，1217s）：cards=224/命中8/详情9/hold=6/resumed=2/orders=14/pindan=3/**vehicle_swapped=1**/route_switched=3。
- 10:17–10:54 多段跑单：新城市网格扫描生效（日志「网格定位-全屏文字」逐屏 OCR 选省/市）。
- 两场均定时收工无异常；拼单账本 + 换车 + 候选 hold/resume 全链路稳定。

## 2) 反思 —— 对照 06 文档
1. **VC++ 运行库冲突是今日最大「坑」（建议补入 06）**：PyQt5 自带旧版 MSVCP140 抢先占坑 → onnxruntime 报 1114 → OCR 全废 → 页面判态失据（不点红色「全国」、不清筛选、乱滑）。**隐蔽**：看似能跑实则全废。已用 spec 剔除 + ctypes 预载双防线根治。
2. **arm_range 不同步 = 09-10「返回箭头点不到」真因**（06·B 类坐标/边角坑）：actuation 更新后 arm_range 没重算，屏幕边角被错误 clamp。今日已做自动同步 + `--sync-arm-range` 补救入口。
3. **Z 安全红线 6.2（碎屏风险）**：z_press 是机械臂坐标，可用窗口仅 0.6mm；断电后 Z 参考漂移最危险。已写进断电规程。
4. **templates.json 双层 `templates/` 路径**：曾误判"会改变识别需重跑基线"，实际 24 条 `enabled:false`——迁移投屏后**有意停用**，非路径错。已更正，避免误判因果。
5. **0 字节 junk 文件**：Windows `>` 重定向残留（`>)` 误建），已删。提醒用 `--output` 而非 `>`。
6. **H 路线 route_setup_fail=2**：设路线偶发失败但跑单仍完成——疑路线解析/等待时序，待查。

## 3) 优化建议（性能 / 鲁棒性 / 可观测性）
- **鲁棒性**：把「VC++ 运行库冲突」「arm_range 不同步」「Z 碎屏红线」三坑补入 `docs/交接文档/06_已知问题`，并加「断电重标 SOP」引用。
- **可观测性→CI 固化**：`build_exe.bat` 末尾加断言——全包同名运行库扫描必须只有 1 套、onnxruntime 必须 IMPORT_OK，否则非零退出（把 gui_boot.log 判据固化进打包）。
- **可观测性**：`analyze_runs.py` 增 `route_setup_fail` / `vehicle_swapped` 维度，量化 H 路线设路线失败根因。
- **资产**：采集 2 张仍缺模板图（offline_close / grabbed_close），消除占位 ambiguity。
- **性能**：detect 每帧 1-2s 全局瓶颈（09-11 起遗留）仍未动，建议按当前页面状态只匹配相关模板。
- **落点补偿**：锚点目前 4 个，建议用整屏网格标定 `--apply` 一次性生成全屏锚点（精度最高）。

## 4) 备份
- `tools/backup_project.py` → **`backups/backup_20260913_230119`**（8344 KB，config/core/tools/docs+根 md/bat）。
- git 已提交 4 笔（7b1e2d5 / 9333168 / 101e09d / 72f7af9）。
- `config/templates.json` 另备份 `config/templates.json.bak_20260913`（路径修复前）。

## 5) 进度文档
- `docs/进度计划.md` 已追加「**7.9、09-13 · 打包交付 + iOS 校准 App + 整屏网格标定 + 落点补偿**」。
