# =============================================================================
# 运满满抢单机器人（iMouse 投屏版）—— 运行依赖清单
#
# 版本 = 本机实测可用组合（2026-09-13，Python 3.11.2 / Windows 10+）。
# ⚠️ 别随手升级：这几个包是互相咬合的，尤其 opencv-python / numpy / onnxruntime /
#    PyQt5 —— 例如 PyQt5 的 wheel 会自带一套旧版 MSVCP140，是"打包后 OCR 全废"的
#    历史事故源头（见 installer/打包与交付说明.md 约定 4）。
#
# 安装：  py -3.11 -m pip install -r requirements.txt
# 自检：  py -3.11 tools\check_deps.py           逐包 import，最贴近真实运行结果
#         py -3.11 tools\check_deps.py --install 缺什么自动补什么
# 一键：  双击 install.bat（= 上面两步，新电脑装环境用这个）
# =============================================================================

# ---- 界面 ----------------------------------------------------------------
PyQt5==5.15.11

# ---- 视觉 / OCR ----------------------------------------------------------
opencv-python==4.11.0.86
numpy==1.26.4
onnxruntime==1.29.0
rapidocr-onnxruntime==1.4.4
# rapidocr 的下游依赖：代码不直接 import，但少了任何一个 OCR 都会废掉，
# 所以一并锁版本（check_deps.py 会逐个 import 验证这条链是否完整）。
pyclipper==1.4.0
PyYAML==6.0.3
Pillow==12.3.0
shapely==2.1.2

# ---- 机械臂（HTTP:8082）/ iMouse ------------------------------------------
requests==2.34.2

# ---- 可选：内存 / 句柄泄漏诊断（默认不装，需要时手动加）--------------------
# core/flow/scanner.py 用它读进程内存；不装也能跑（有 ctypes 兜底，字段显示 -1）。
# 为什么不写进本清单：tools/check_deps.py 会把清单里每一项都 import 一遍，
# 若把「可选包缺失」也算失败，新电脑上就会把"环境是好的"误报成"装不上"。
# 需要时手工装：py -3.11 -m pip install psutil
# psutil

# ---- 导入顺序约束（写在这里是为了留个显眼的位置）----------------------------
# onnxruntime 必须**早于** PyQt5 导入：PyQt5 的 wheel 自带一套旧版 MSVCP140.dll，
# 先加载它会让 onnxruntime 报 [WinError 1114] 初始化例程失败。
# gui/app.py 已用模块级代码保证；tools/check_deps.py 也显式把它提到最前。
# ⚠️ 自己写新脚本时同样注意：先 import onnxruntime 再 import PyQt5。

# ---- 仅打包用，运行不需要 --------------------------------------------------
# PyInstaller==6.22.2
